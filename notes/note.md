# KPI 
1. Use hours instead of days as measurement metric, since at date change boundaries a minute change will yield a day's differnce, using hours, the error is bounded max by an hour. in a logistics setting an hour is acceptable.

